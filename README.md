# Workspace Overview

This repository now includes `recovery-ai/`, a fully local scaffold for offline entropy and recovery-model experimentation. See `recovery-ai/README.md` for setup, pipeline order, and script entry points.
